# 阶段 3：主窗口 MemoryRouter 迁移

- 状态：已完成；首页列表缓存补充人工验收通过
- 预计工时：2-3 单人工作日
- 实际工时：本次执行约 0.1 小时，不含人工回归
- 开始时间：2026-06-11 14:36:33 +08:00
- 完成时间：实现于 2026-06-11 14:40:27 +08:00 完成；用户于 2026-06-11 确认人工验收通过
- 依赖阶段：阶段 2
- 阻塞项：无

## 目标

- 使用 `react-router-dom@7.17.0` 管理主窗口内部页面导航。
- 删除 `showSettings`、`showTagManager`、`settingsSubpage` 形成的重复页面真相。

## 执行清单

- [x] 单独安装并提交 `react-router-dom@7.17.0`。
- [x] 保留 `main.tsx` 的多窗口查询参数识别。
- [x] 仅为主窗口挂载 `MemoryRouter`。
- [x] React Router 仅替换现有页面布尔状态，不扩展到独立窗口或其他导航场景。
- [x] 建立 `/`、`/tags`、`/settings`、`/settings/advanced` 路由。
- [x] 未知路径重定向到 `/`。
- [x] 将标题、返回按钮、页面显示和全局导航开关改为读取当前路由。
- [x] 收到 `focus-search-input` 时导航首页并聚焦。
- [x] 标签管理关闭时保护 `/tags` 路由。
- [x] 删除对应页面布尔状态和兼容分支。
- [x] 记录依赖引入前后的构建产物体积。
- [x] 确认未增加三项已批准例外之外的依赖。

## 验收场景

- 首页、标签、设置和高级设置可以进入。
- 高级设置返回设置首页，其余页面返回首页。
- 搜索快捷键从任意页面回到首页并聚焦搜索框。
- 标签管理关闭后不能停留在 `/tags`。
- 独立高级设置窗口仍可正常加载。
- 路由迁移后的页面真相数量和导航分支少于迁移前。

### 首页列表缓存补充

- [x] 使用 Virtuoso 官方状态快照保存首页列表滚动位置和已测量项尺寸。
- [x] 仅在离开首页导致列表卸载时保存快照。
- [x] 返回首页时恢复快照，不让首页 DOM 常驻。
- [x] 首页为空列表时清除旧快照，避免恢复无效状态。
- [x] 快照仅保存在当前主窗口内存，不进入 Zustand 或持久化。
- [x] 执行设置页、标签页往返后的桌面端人工回归。

## 验证结果

- `npm test`：通过，32 个测试。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过。
- `git diff --check`：通过。
- Router 引入前构建体积：主 JS 767.48 kB，gzip 235.50 kB
- Router 引入后构建体积：主 JS 801.00 kB，gzip 247.29 kB；增加 33.52 kB，gzip 增加 11.79 kB
- 依赖检查：通过；仅新增固定版本 `react-router-dom@7.17.0` 及其锁文件依赖。
- Rust、样式与独立窗口边界检查：通过；未修改 Rust 源码或样式，独立高级设置窗口仍由查询参数直接渲染。
- 人工路由回归：用户于 2026-06-11 确认验收通过。
- 轻量化验收：通过；Router 仅用于主窗口页面导航，旧页面状态已删除，现有业务 hooks 继续使用路由派生布尔值。
- 首页列表缓存补充：实现于 2026-06-11 14:58:44 +08:00 完成；`npm test` 42 个测试通过；`npx tsc --noEmit`、`npm run build` 与 `git diff --check` 通过。
- 首页列表缓存构建体积：主 JS 801.39 kB，gzip 247.42 kB；相较补充前增加 0.39 kB，gzip 增加 0.13 kB。
- 首页列表缓存边界检查：通过；仅使用 `VirtualClipboardList` 的内存快照接口，未修改路由、Zustand、历史分页、样式或 IPC。
- 首页列表缓存人工回归：用户于 2026-06-12 确认通过。

## 相关提交

- `chore: add react router dependency`
- `refactor: migrate main window navigation to memory router`
- `fix: preserve home list state across navigation`

## 回滚方式

- 依赖提交和路由迁移提交分离。
- 路由迁移失败时回滚路由提交，恢复原页面状态分支。

## 遗留问题

- 首页列表缓存补充的桌面端人工回归尚未执行。
