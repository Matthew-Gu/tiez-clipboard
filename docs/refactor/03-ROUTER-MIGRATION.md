# 阶段 3：主窗口 MemoryRouter 迁移

- 状态：进行中
- 预计工时：2-3 单人工作日
- 实际工时：待记录
- 开始时间：2026-06-11 14:36:33 +08:00
- 完成时间：待记录
- 依赖阶段：阶段 2
- 阻塞项：无

## 目标

- 使用 `react-router-dom@7.17.0` 管理主窗口内部页面导航。
- 删除 `showSettings`、`showTagManager`、`settingsSubpage` 形成的重复页面真相。

## 执行清单

- [ ] 单独安装并提交 `react-router-dom@7.17.0`。
- [ ] 保留 `main.tsx` 的多窗口查询参数识别。
- [ ] 仅为主窗口挂载 `MemoryRouter`。
- [ ] React Router 仅替换现有页面布尔状态，不扩展到独立窗口或其他导航场景。
- [ ] 建立 `/`、`/tags`、`/settings`、`/settings/advanced` 路由。
- [ ] 未知路径重定向到 `/`。
- [ ] 将标题、返回按钮、页面显示和全局导航开关改为读取当前路由。
- [ ] 收到 `focus-search-input` 时导航首页并聚焦。
- [ ] 标签管理关闭时保护 `/tags` 路由。
- [ ] 删除对应页面布尔状态和兼容分支。
- [ ] 记录依赖引入前后的构建产物体积。
- [ ] 确认未增加三项已批准例外之外的依赖。

## 验收场景

- 首页、标签、设置和高级设置可以进入。
- 高级设置返回设置首页，其余页面返回首页。
- 搜索快捷键从任意页面回到首页并聚焦搜索框。
- 标签管理关闭后不能停留在 `/tags`。
- 紧凑预览和独立高级设置窗口仍可正常加载。
- 路由迁移后的页面真相数量和导航分支少于迁移前。

## 验证结果

- `npm test`：待执行
- `npx tsc --noEmit`：待执行
- `npm run build`：待执行
- Router 引入前构建体积：主 JS 767.48 kB，gzip 235.50 kB
- Router 引入后构建体积：待记录
- 人工路由回归：待执行
- 轻量化验收：确认 Router 未扩展至主窗口页面导航之外

## 相关提交

- `chore: add react router dependency`
- `refactor: migrate main window navigation to memory router`

## 回滚方式

- 依赖提交和路由迁移提交分离。
- 路由迁移失败时回滚路由提交，恢复原页面状态分支。

## 遗留问题

- 待记录
