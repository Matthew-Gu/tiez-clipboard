# 阶段 2：Tauri IPC 契约集中化

- 状态：未开始
- 预计工时：2-3 单人工作日
- 实际工时：待记录
- 开始时间：待记录
- 完成时间：待记录
- 依赖阶段：阶段 1
- 阻塞项：无

## 目标

- 集中管理前端使用的 Tauri 命令名、事件名、设置键和参数类型。
- 保持现有 Rust 命令、参数、返回数据与事件负载完全不变。

## 执行清单

- [ ] 建立命令、事件和设置键常量模块。
- [ ] 按 clipboard、settings、window、system、tag 建立薄 service。
- [ ] 为高频命令定义请求与返回类型。
- [ ] 优先迁移设置、历史分页和窗口导航调用。
- [ ] 按业务域逐步替换散落的字符串调用。
- [ ] 禁止 service 内引入业务状态或 UI 逻辑。
- [ ] 补充关键 IPC 请求参数的契约测试。

## 兼容要求

- 不修改命令名、参数名、大小写和可选参数行为。
- 不修改事件名与事件 payload。
- 不修改错误字符串处理方式。
- 不在本阶段修改 Rust 业务实现。

## 验证结果

- `npm test`：待执行
- `npx tsc --noEmit`：待执行
- `cargo test`：如未修改 Rust，可标记不适用
- IPC 参数对照检查：待执行

## 相关提交

- 建议：`refactor: centralize tauri command contracts`

## 回滚方式

- 按业务域拆分提交，任一 service 迁移可独立回滚到直接 `invoke/listen`。

## 遗留问题

- 待记录

