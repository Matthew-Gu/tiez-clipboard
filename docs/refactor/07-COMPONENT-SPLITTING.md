# 阶段 7：高复杂度组件拆分

- 状态：已完成
- 预计工时：6-9 单人工作日
- 实际工时：本次执行约 0.3 小时，不含人工回归
- 开始时间：2026-06-12 09:43:34 +08:00
- 完成时间：实现于 2026-06-12 10:00:25 +08:00 完成；用户于 2026-06-12 人工验收通过
- 依赖阶段：阶段 6
- 阻塞项：需要稳定的路由、Store 和 IPC 契约作为前置条件

## 目标

- 拆分大型组件的展示、业务行为和窗口生命周期职责。
- 优先提取纯函数和无状态展示组件，保持现有 DOM 结构、样式类名和行为。

## 执行顺序

- [x] 拆分前记录目标组件的明确复杂度问题和预期收益，无明确收益时延期。
- [x] 仅提取能显著降低主体复杂度的主列表富文本图片标记、表格来源识别和颜色识别纯函数。
- [x] 为提取的主列表内容展示判定函数补充测试。
- [x] 按内容类型拆分 ClipboardItem 的主列表内容展示。
- [x] 拆分 ClipboardItem 标签编辑与操作区。
- [x] 拆分 TagManager 的数据行为、侧栏、条目区和对话框。
- [x] 拆分复杂设置组中的重复控制结构。
- [x] 保持现有 CSS 类名和可访问属性。
- [x] 每次拆分仅做代码移动和依赖收窄，不同时改变业务行为。
- [x] 不把简单函数拆成多个文件，不将只使用一次的逻辑抽成公共工具。
- [x] 不创建超过现有业务域所需的额外目录层级。
- [x] 不进行无关命名、格式化或风格统一。

## 高风险约束

- 不调整富文本降级策略。
- 不恢复或修改已经移除的紧凑预览窗口。
- 不修改标签删除、重命名和敏感标签保护规则。
- 不改变固定项拖拽排序行为。
- 拆分后文件数量、目录层级和调用链增长必须有明确复杂度收益支撑。

## 验收场景

- 主列表所有内容类型展示与拆分前一致。
- 主列表显示行为保持一致。
- 标签管理增删改查和批量操作一致。
- 组件拆分后没有新增循环依赖或 `shared -> features` 反向依赖。

## 验证结果

- 纯函数测试：通过；新增 5 个测试，覆盖富文本图片标记、GIF、表格来源和独立颜色识别。
- `npm test`：通过，53 个测试。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；主 JS 780.17 kB，gzip 241.83 kB。
- `git diff --check`：通过。
- 主列表内容展示、标签编辑、标签管理和快捷键录入人工回归：用户于 2026-06-12 确认通过。
- 轻量化验收：通过；新模块均保留在所属 feature 内，没有新增 `shared -> features` 反向依赖；`ClipboardItem.tsx` 从 1109 行降至 444 行，`ClipboardSettingsGroup.tsx` 从 762 行降至 639 行；`TagManager.tsx` 的数据与业务 UI 已拆出，剩余长度主要为阶段 8 保留的内嵌样式。

## 相关提交

- `refactor: extract clipboard display helpers`
- `refactor: split clipboard item responsibilities`
- `refactor: split tag manager responsibilities`

## 回滚方式

- 每个职责拆分独立提交。
- 出现行为或视觉回归时回滚最后一个拆分提交。

## 遗留问题

- `TagManager.tsx` 内嵌样式保持不变，按计划留到阶段 8 处理。
