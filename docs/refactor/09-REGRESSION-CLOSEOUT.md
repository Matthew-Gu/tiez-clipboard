# 阶段 9：完整回归与计划收尾

- 状态：未开始
- 预计工时：2-3 单人工作日
- 实际工时：待记录
- 开始时间：待记录
- 完成时间：待记录
- 依赖阶段：阶段 8
- 阻塞项：所有前置阶段必须通过阶段门禁

## 目标

- 对完整重构结果执行自动化、构建和桌面运行时回归。
- 记录残余风险、构建产物变化和回滚点，关闭计划。

## 自动化验证

- [ ] `npx tsc --noEmit`
- [ ] `npm test`
- [ ] `cargo test`
- [ ] `npm run build`
- [ ] `npm run tauri:build`
- [ ] 检查 `git diff` 无无关改动
- [ ] 记录构建产物体积变化
- [ ] 记录 Less 接入前后最终 CSS 体积和构建时间变化
- [ ] 按总控计划“重构前基线数据”的相同范围、扩展名、统计方式和过大文件阈值记录“重构后数据”
- [ ] 对比重构前后的源码行数、源码文件数、过大文件数、依赖数、主要目录代码量和明显重复逻辑簇

## 轻量化验收

- [ ] 确认除 `react-router-dom@7.17.0`、`zustand@5.0.14`、`less@4.6.4` 外没有新增公共依赖。
- [ ] 确认 React Router、Zustand 和 Less 未扩大到计划边界之外。
- [ ] 确认没有新增过深目录、机械 service、一次性公共工具或一次性 selector/mixin。
- [ ] 确认新增抽象能够删除重复逻辑、收敛配置、缩短调用链或降低组件复杂度。
- [ ] 确认重构提交未混入无关命名、格式化或风格统一。
- [ ] 记录删除的无用代码、合并的重复逻辑和简化的调用链。
- [ ] 确认重构后基线统计未调整目录范围、文件扩展名、行数口径或过大文件阈值。

## 功能回归

- [ ] 文本、代码、URL、图片、文件和视频捕获；浏览器、Office 等格式化文本来源仅保存纯文本。
- [ ] 普通粘贴、右键纯文本粘贴、顺序粘贴和快捷粘贴。
- [ ] 搜索、标签搜索、类型筛选和分页加载。
- [ ] 标签增删改、固定、排序和删除。
- [ ] 隐私识别、敏感内容模糊、显示和加密行为。
- [ ] 所有设置修改、保存、重启恢复和跨窗口同步。
- [ ] 主窗口路由、返回、搜索快捷键和标签路由保护。
- [ ] 独立高级设置窗口。
- [ ] 全新数据库初始化后表、索引、默认设置和内置标签正确。
- [ ] 重复启动验证当前数据库结构初始化幂等，不重复创建或失败。

## 视觉回归

- [ ] 六套主题亮色模式。
- [ ] 六套主题暗色模式。
- [ ] 主窗口、标签管理、设置和弹窗。

## 发布形态回归

- [ ] NSIS 安装版启动与数据目录行为。
- [ ] 便携版启动与 `tiez-data` 数据目录行为。
- [ ] 托盘、全局快捷键、窗口置顶、跟随鼠标和失焦隐藏。

## 验证结果

- 待记录

## 相关提交

- `test: complete refactor regression coverage`
- `docs: close stable refactor plan`

## 回滚方式

- 根据失败功能定位至最后一个通过验证的阶段提交。
- 使用 `git revert` 回滚问题阶段。
- 本轮不兼容旧数据库结构；回滚或跨版本使用数据库不在验收范围内。
- 核心桌面行为失败时停止发布，不使用临时绕过关闭验证。

## 遗留问题

- 待记录
