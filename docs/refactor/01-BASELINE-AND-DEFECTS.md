# 阶段 0-1：基线与缺陷隔离

- 状态：阶段 0 已完成；阶段 1 未开始
- 预计工时：2.5-4 单人工作日
- 实际工时：待记录
- 开始时间：2026-06-11
- 完成时间：阶段 0 于 2026-06-11 完成；阶段 1 待记录
- 依赖阶段：无
- 阻塞项：敏感标签最终业务定义需要在实施前再次确认

## 目标

- 固化重构前可观察行为和自动化基线。
- 将现有业务缺陷与结构重构分离，避免后续无法判断回归来源。

## 执行清单

- [ ] 记录主窗口、标签、设置、高级设置、紧凑预览窗口的现有行为。
- [ ] 为设置解析、页面导航判断、敏感标签判断补充特征测试。
- [ ] 验证粘贴音效设置是否错误写入 `delete_after_paste` 状态。
- [ ] 使用独立 `fix` 提交修复粘贴音效状态映射。
- [ ] 确认 `password` 是否属于后端加密敏感标签。
- [ ] 使用独立 `fix` 提交统一敏感标签行为。
- [ ] 重新运行全部基线检查并记录结果。

## 已知缺陷候选

1. Rust 设置命令处理 `app.sound_paste_enabled` 时疑似更新了 `delete_after_paste` 状态。
2. 前端将 `password` 视为敏感标签，但后端加密标签仅包含 `sensitive` 和 `密码`。

## 验证结果

- `npm test`：待执行
- `npx tsc --noEmit`：待执行
- `cargo test`：待执行
- 人工行为基线：待记录

## 相关提交

- 待记录

## 回滚方式

- 测试提交可独立保留。
- 每个缺陷使用独立提交，通过 `git revert <commit>` 单独回滚。

## 遗留问题

- 待记录
